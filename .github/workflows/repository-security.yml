name: Repository security

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  reject-secret-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run security:repository
