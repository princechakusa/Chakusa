import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { appointmentListSchema, createAppointmentSchema, transitionAppointmentSchema, updateAppointmentSchema } from "./appointments.schemas.js";
import { createAppointment, getAppointment, listAppointments, transitionAppointment, updateAppointment } from "./appointments.service.js";
const idParams = z.object({ id: z.string().uuid() });
export default async function appointmentRoutes(fastify: FastifyInstance) { fastify.addHook("preHandler", fastify.authenticate); fastify.addHook("preHandler", fastify.requireBusiness); fastify.get("/", async (request, reply) => reply.send(await listAppointments(request.businessId!, appointmentListSchema.parse(request.query)))); fastify.post("/", async (request, reply) => reply.status(201).send(await createAppointment(request.businessId!, request.user.userId, createAppointmentSchema.parse(request.body)))); fastify.get("/:id", async (request, reply) => reply.send(await getAppointment(request.businessId!, idParams.parse(request.params).id))); fastify.patch("/:id", async (request, reply) => reply.send(await updateAppointment(request.businessId!, request.user.userId, idParams.parse(request.params).id, updateAppointmentSchema.parse(request.body)))); fastify.post("/:id/status", async (request, reply) => reply.send(await transitionAppointment(request.businessId!, request.user.userId, idParams.parse(request.params).id, transitionAppointmentSchema.parse(request.body).status))); }
