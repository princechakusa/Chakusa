import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { appointmentListSchema, appointmentPaymentSchema, createAppointmentSchema, transitionAppointmentSchema, updateAppointmentSchema } from "./appointments.schemas.js";
import { createAppointment, getAppointment, listAppointments, transitionAppointment, updateAppointment, updateAppointmentPayment } from "./appointments.service.js";
import { sendAppointmentConfirmation } from "./appointmentReminders.js";
import { ApiError } from "../../lib/errors.js";
const idParams = z.object({ id: z.string().uuid() });
export default async function appointmentRoutes(fastify: FastifyInstance) { fastify.addHook("preHandler", fastify.authenticate); fastify.addHook("preHandler", fastify.requireBusiness); fastify.get("/", async (request, reply) => reply.send(await listAppointments(request.businessId!, appointmentListSchema.parse(request.query)))); fastify.post("/", async (request, reply) => reply.status(201).send(await createAppointment(request.businessId!, request.user.userId, createAppointmentSchema.parse(request.body)))); fastify.get("/:id", async (request, reply) => reply.send(await getAppointment(request.businessId!, idParams.parse(request.params).id))); fastify.patch("/:id", async (request, reply) => reply.send(await updateAppointment(request.businessId!, request.user.userId, idParams.parse(request.params).id, updateAppointmentSchema.parse(request.body)))); fastify.post("/:id/status", async (request, reply) => reply.send(await transitionAppointment(request.businessId!, request.user.userId, idParams.parse(request.params).id, transitionAppointmentSchema.parse(request.body).status))); fastify.patch("/:id/payment", async (request, reply) => reply.send(await updateAppointmentPayment(request.businessId!, request.user.userId, idParams.parse(request.params).id, appointmentPaymentSchema.parse(request.body).paidAmount))); fastify.post("/:id/send-confirmation", async (request, reply) => { const id = idParams.parse(request.params).id; await getAppointment(request.businessId!, id); if (!await sendAppointmentConfirmation(id)) throw ApiError.conflict("Confirmation could not be sent. Check the plan, customer phone, opt-out status, or previous delivery."); reply.send(await getAppointment(request.businessId!, id)); }); }
