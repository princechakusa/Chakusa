import { z } from "zod";
const dateTime = z.string().datetime({ offset: true });
export const appointmentStatusSchema = z.enum(["SCHEDULED", "CONFIRMED", "COMPLETED", "CANCELED", "NO_SHOW"]);
export const appointmentListSchema = z.object({ from: dateTime, to: dateTime, status: appointmentStatusSchema.optional(), customerId: z.string().uuid().optional() }).refine(value => new Date(value.to) > new Date(value.from), { message: "to must be after from", path: ["to"] });
export const createAppointmentSchema = z.object({ customerId: z.string().uuid().optional(), assignedMemberId: z.string().uuid().optional(), serviceOfferingId: z.string().uuid().optional(), serviceName: z.string().trim().min(1).max(120), startsAt: dateTime, endsAt: dateTime, price: z.number().nonnegative().max(99_999_999).optional(), notes: z.string().trim().max(2000).optional(), reminderMinutes: z.number().int().min(0).max(10_080).nullable().optional() }).refine(value => new Date(value.endsAt) > new Date(value.startsAt), { message: "endsAt must be after startsAt", path: ["endsAt"] });
export const updateAppointmentSchema = z.object({ customerId: z.string().uuid().nullable().optional(), assignedMemberId: z.string().uuid().nullable().optional(), serviceOfferingId: z.string().uuid().nullable().optional(), serviceName: z.string().trim().min(1).max(120).optional(), startsAt: dateTime.optional(), endsAt: dateTime.optional(), price: z.number().nonnegative().max(99_999_999).nullable().optional(), notes: z.string().trim().max(2000).nullable().optional(), reminderMinutes: z.number().int().min(0).max(10_080).nullable().optional() });
export const transitionAppointmentSchema = z.object({ status: appointmentStatusSchema });
export const appointmentPaymentSchema = z.object({ paidAmount: z.number().nonnegative().max(99_999_999) });
export const bulkImportAppointmentsSchema = z.object({ appointments: z.array(z.object({ customerName: z.string().trim().min(1).max(120), customerPhone: z.string().trim().optional(), customerEmail: z.string().trim().email().optional(), serviceName: z.string().trim().min(1).max(120), startsAt: dateTime, endsAt: dateTime, price: z.number().nonnegative().max(99_999_999).optional(), notes: z.string().trim().max(2000).optional() }).refine(value => new Date(value.endsAt) > new Date(value.startsAt), { message: "endsAt must be after startsAt", path: ["endsAt"] })).min(1).max(250, "Import is limited to 250 appointments at a time") });
export type CreateAppointmentInput = z.infer<typeof createAppointmentSchema>;
export type UpdateAppointmentInput = z.infer<typeof updateAppointmentSchema>;
export type BulkImportAppointmentsInput = z.infer<typeof bulkImportAppointmentsSchema>;
