import { defineConfig } from "astro/config";
// Reserved placeholder; replace only after the production domain is confirmed.
export default defineConfig({ output: "static", site: "https://www.chakusa.example" });
